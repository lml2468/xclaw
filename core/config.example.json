{
  "_comment": "XClaw global config. Place at ~/.xclaw/config.json. Shared defaults + the list of bots to run. NEVER put a token here — tokens live in ~/.xclaw/<id>/config.json.",
  "apiUrl": "https://your-octo-server.example",
  "context": { "maxContextChars": 6000 },
  "rateLimit": { "maxPerMinute": 5 },
  "bots": [
    { "id": "default" }
  ]
}
