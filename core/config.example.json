{
  "_comment": "OctoBuddy config. Single file at ~/.octobuddy/config.json. Top-level rateLimit/context are shared runtime policy. Bot identity, API URL, agent routing, env, group policy, and persona-clone config are per-bot only. Secrets are not written here: config stores secretRef entries that are resolved from the secret backend at runtime.",
  "rateLimit": { "maxPerMinute": 30 },
  "context": { "maxContextChars": 6000 },
  "bots": [
    {
      "id": "my-bot",
      "apiUrl": "https://your-octo-server.example",
      "_comment_tokens": "octoToken and agent.gatewayToken are intentionally absent. The desktop app stores them in the secret backend and injects them into octobuddy-daemon at runtime. Headless deployments may still provide inline tokens if they accept config-file exposure.",
      "agent": {
        "model": "claude-opus-4-8",
        "gatewayBaseUrl": "https://your-model-gateway.example/v1",
        "_comment_cron": "Set cron=true to enable this bot's scheduled tasks (#115): owner-gated cron.create/list/delete over the control bus, fired through the gateway. Off by default.",
        "cron": false,
        "_comment_toolProgress": "Opt-in: when true, the bot mirrors each tool it runs to the channel as a brief 'Running <tool>(<params>)...' notice (deduped, capped at 10/turn). Off by default.",
        "toolProgress": false,
        "_comment_inheritUserConfig": "When true, the agent inherits the operator's ~/.claude (user-scope skills + installed plugins). OFF by default: each bot gets an isolated CLAUDE_CONFIG_DIR (~/.octobuddy/<id>/.claude).",
        "inheritUserConfig": false,
        "_comment_dispatchTimeoutSec": "Per-turn IDLE timeout (seconds). Omit to use the daemon default (1200 = 20 min).",
        "dispatchTimeoutSec": 0,
        "_comment_systemPromptMode": "How SOUL.md+AGENTS.md are sent to claude. 'minimal' (default): REPLACE the built-in system prompt; cwd .claude/ is not auto-loaded; tool surface is a headless whitelist. 'claude_code': APPEND to the built-in prompt; cwd .claude/ auto-loads; bypassPermissions. Use claude_code only if your SOUL was written assuming the built-in preamble.",
        "systemPromptMode": "minimal",
        "_comment_settingSources": "Which claude filesystem setting scopes load (minimal mode). Allowed: 'user', 'project'. Omit/empty = ['user'] (default: per-bot skills under CLAUDE_CONFIG_DIR load; project/local dropped). Adding 'project' loads the sandbox cwd's .claude/ + CLAUDE.md — a prompt-injection vector for untrusted group chats; enable only for trusted single-operator bots. 'local' is not allowed.",
        "settingSources": ["user"],
        "_comment_tools": "Tool surface. Omit to use the binary's probed headless-safe set (built-in tools minus interactive ones). tools.default = bot-level whitelist; tools.channels keys by sessionKey (group = channelId; DM = '<spaceId>:<uid>' or bare uid) and overrides the default for that conversation (empty list = muzzle that channel). Unconfigured channels and the desktop Console use the probed default. Tool names: letters/digits/_.*- only (no commas or spaces).",
        "tools": {
          "default": ["Read", "Edit", "Write", "Bash", "Grep", "Glob"],
          "channels": {
            "some-channel-id": ["Read", "Grep", "Glob"]
          }
        },
        "_comment_env": "Env is per-bot. Use {\"value\":\"...\"} for non-secret values and {\"secretRef\":\"env/NAME\"} for values stored in the secret backend.",
        "env": {
          "OCTO_BOT_ID": { "value": "this-bot-octo-id" },
          "GH_TOKEN": { "secretRef": "env/GH_TOKEN" }
        }
      },
      "_comment_groupConfigDir": "Optional operator-controlled directory of per-conversation instruction files. Must be outside this bot's agent-writable workspace.",
      "groupConfigDir": "/etc/octobuddy/groups/my-bot",
      "_comment_gating": "Optional per-bot group-gating policy (session-router). trigger.mentionFreeGroups = channel ids the bot answers WITHOUT an @mention; knownBotUids/allowedBotUids = bot-loop guard; botBlocklist = uids whose DMs are dropped.",
      "knownBotUids": [],
      "allowedBotUids": [],
      "botBlocklist": [],
      "_comment_trigger": "Per-bot trigger pipeline policy (issue #105). aiBroadcast = deny|allowlist|allow (default deny — pure @AI without @mention/@all does not trigger). replyToBotEnabled = true|false (default true — quote-replies to the bot continue the thread). mentionFreeGroups = channel ids that answer without an @mention.",
      "trigger": {
        "aiBroadcast": "deny",
        "replyToBotEnabled": true,
        "mentionFreeGroups": []
      },
      "_comment_skills": "Skills/workflows are NOT configured here. Each bot owns its own under ~/.octobuddy/<id>/.claude/skills/<name>/ and ~/.octobuddy/<id>/.claude/workflows/<name>.js.",
      "_comment_mcp": "MCP servers are NOT configured here either. Write a standard mcp.json to ~/.octobuddy/<id>/.claude/.mcp.json; the daemon loads it per turn via --mcp-config --strict-mcp-config. The desktop manages it (with a connection test) on the bot's 基础信息 page. To expose MCP tools, include matching tool names (or mcp__*) in agent.tools."
    },
    {
      "id": "admin-clone",
      "apiUrl": "https://your-octo-server.example",
      "_comment_persona": "Persona clone (openclaw OBO): replies in the grantor's voice. uid is the grantor's server uid; name is woven into the persona system prompt; personaPrompt is optional.",
      "onBehalfOf": {
        "uid": "u_admin_grantor_uid",
        "name": "Admin",
        "personaPrompt": "Always reply concisely and in English."
      }
    }
  ]
}
